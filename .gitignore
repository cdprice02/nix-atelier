result
result-*

# Secrets and local certs — never commit
secrets.env
.config/secrets/
.certs/

# Claude Code runtime data — everything except a tracked project skill
.claude/*
!.claude/skills/
!.claude/skills/**

# Legacy per-machine identity file (pre-#122). No longer read by anything;
# ignored so a stray copy left over from before v3 can never be committed.
user.nix
